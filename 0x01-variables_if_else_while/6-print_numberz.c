#include <stdio.h>
/**
 * main - other numbers but just use patchar dont use printf
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 11; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
